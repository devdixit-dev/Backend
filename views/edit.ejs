<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>Edit post</title>
</head>

<body class="w-screen h-screen bg-zinc-800 text-white px-4">
  <div class="flex justify-end items-center text-center">
    <form action="/user/logout" method="post">
      <button type="submit" class="mt-4 text-sm px-4 py-2 bg-red-500 rounded-sm cursor-pointer">Logout</button>
    </form>
  </div>

  <div>
    <h2 class="mb-4">Edit your post</h2>
    <form action="/user/update/<%= post._id %>" method="post">
      <textarea name="content"
        class="w-1/3 min-h-30 px-4 py-3 outline-none resize-none bg-transparent border-2 border-zinc-500 rounded-md"
        placeholder="What's on your mind?"><%= post.content %></textarea>
      <button type="submit" class="block text-sm w-40 mt-4 px-4 py-2 bg-yellow-500 text-black rounded-sm cursor-pointer">Update your
        post</button>
    </form>
  </div>
</body>

</html>